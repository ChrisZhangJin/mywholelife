---
phase: 02-hot-path-write-init
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/mywholelife/main.go
  - cmd/mywholelife/serve.go
  - server/config.go
  - server/router.go
  - server/register.go
  - server/register_test.go
autonomous: true
requirements: [WRITE-01]

must_haves:
  truths:
    - "`mywholelife serve` starts a Gin HTTP server on MWL_ADDR (D-02)"
    - "`POST /agent/register` with an X-Agent-Name header mints a new agent and returns its UUID as a bare text/plain body (D-10, register-first resolution of OQ-1)"
    - "A duplicate agent name returns 409; a missing name returns 400"
    - "The cobra root exposes `serve` (this phase) and a reserved `dream` stub (D-01)"
    - "Nothing outside store/ imports the sqlite driver — the Phase-1 seam test stays green (D-01)"
  artifacts:
    - path: "cmd/mywholelife/main.go"
      provides: "cobra root command wiring serve + dream stub, calls Execute"
      contains: "cobra.Command"
    - path: "cmd/mywholelife/serve.go"
      provides: "serve subcommand: FromEnv -> NewBlobStore -> store.Open -> NewRouter -> Run"
      contains: "store.Open"
    - path: "server/config.go"
      provides: "Config struct + FromEnv reading MWL_ADDR/MWL_DATA_ROOT/MWL_DB_PATH with defaults"
      contains: "func FromEnv"
    - path: "server/router.go"
      provides: "NewRouter(store.MemoryStore, store.BlobStore) *gin.Engine with /agent/register wired"
      exports: ["NewRouter"]
    - path: "server/register.go"
      provides: "registerAgent handler returning bare UUID text/plain"
      contains: "RegisterAgent"
  key_links:
    - from: "cmd/mywholelife/serve.go"
      to: "store.Open / store.NewBlobStore"
      via: "constructor calls through the store package (seam intact)"
      pattern: "store\\.(Open|NewBlobStore)"
    - from: "server/register.go"
      to: "store.MemoryStore.RegisterAgent"
      via: "handler calls the store interface, writes Agent.ID as text/plain"
      pattern: "RegisterAgent"
---

<objective>
Stand up the `mywholelife` binary and HTTP surface: a cobra CLI (`serve` + reserved `dream` stub), env-driven `Config`, a Gin router constructed over the Phase-1 `store` interfaces, and the register-first identity endpoint `POST /agent/register`.

Purpose: Establish the seam-preserving server scaffold that Plan 02 (write + init handlers) extends, and resolve OQ-1 by making agent identity a server-minted UUID obtained before any write. Handlers stay thin (D-01); only `store/` touches disk or SQL.

Output: A buildable, runnable server whose only route is `POST /agent/register`, returning a bare UUID the client persists in `agent.json` (D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hot-path-write-init/02-CONTEXT.md
@.planning/phases/02-hot-path-write-init/02-RESEARCH.md

<interfaces>
<!-- From store/memorystore.go and store/sqlite.go (Phase 1) — use directly, do NOT re-implement. -->

store.MemoryStore.RegisterAgent(ctx, name string) (store.Agent, error)   // mints a NEW server uuid; does NOT accept a caller id
store.MemoryStore.GetAgent(ctx, id string) (store.Agent, error)          // returns store.ErrNotFound if unknown
store.Agent{ ID string; Name string; CreatedAt int64 }
store.ErrDuplicateName  // returned by RegisterAgent on a UNIQUE name collision
store.ErrNotFound

func store.Open(dbPath string, blobs store.BlobStore) (*sqliteStore, error)  // returns a concrete type usable as store.MemoryStore
func store.NewBlobStore(root string) *localBlobStore                         // usable as store.BlobStore
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gin + cobra, build the CLI, config, router and register endpoint</name>
  <files>go.mod, go.sum, cmd/mywholelife/main.go, cmd/mywholelife/serve.go, server/config.go, server/router.go, server/register.go</files>
  <read_first>
    - 02-RESEARCH.md §"Gin router + serve wiring (via cobra)" (cobra/serve/NewRouter shape)
    - 02-RESEARCH.md §"Standard Stack" (gin v1.12.0, cobra v1.10.2 — verified live on goproxy.cn) and §"Dependency-free client idioms" (register returns a BARE text/plain UUID)
    - 02-RESEARCH.md §"Open Questions" OQ-1 (register-first is the chosen resolution)
    - store/memorystore.go, store/sqlite.go (RegisterAgent/GetAgent signatures, ErrDuplicateName, ErrNotFound, store.Open, store.NewBlobStore)
  </read_first>
  <action>
    Add dependencies with GOPROXY already set to goproxy.cn: `go get github.com/gin-gonic/gin@v1.12.0` and `go get github.com/spf13/cobra@v1.10.2`.

    Create `server/config.go`: a `Config` struct { Addr, DataRoot, DBPath string } and `func FromEnv() Config` reading `MWL_ADDR` (default `:8080`), `MWL_DATA_ROOT` (default `./data`), and `MWL_DB_PATH` (default `filepath.Join(DataRoot, "mywholelife.db")`) via `os.Getenv` per D-02. No auth — the `{id}` path is the entire scope.

    Create `server/router.go`: `func NewRouter(st store.MemoryStore, bl store.BlobStore) *gin.Engine` that does `gin.New()`, `Use(gin.Recovery())`, and wires exactly one route this plan: `r.POST("/agent/register", registerAgent(st))`. Leave the two data routes for Plan 02 (do not reference undefined handlers — the package must compile). Import only the `store` interfaces plus gin — never the sqlite driver (the Phase-1 seam test walks the whole repo).

    Create `server/register.go`: `func registerAgent(st store.MemoryStore) gin.HandlerFunc`. Read the agent name from the `X-Agent-Name` header; if empty return 400. Call `st.RegisterAgent(c, name)`; on `store.ErrDuplicateName` return 409; on other error return 500; on success write `agent.ID` as a bare body with `c.String(http.StatusOK, agent.ID)` (Content-Type text/plain) so the client parses it with plain `$(curl ...)` — no jq (D-10, resolves OQ-1/A2). Do NOT accept a caller-supplied id anywhere: RegisterAgent mints its own UUID.

    Create `cmd/mywholelife/main.go`: a cobra root `&cobra.Command{Use: "mywholelife"}`, `AddCommand(serveCmd, dreamCmd)` where `dreamCmd` is a reserved stub (`Use: "dream"`, `RunE` returns an error like "dream: not implemented until phase 4"), then `root.Execute()` in `main()`. Create `cmd/mywholelife/serve.go`: `serveCmd` whose `RunE` calls `server.FromEnv()`, `store.NewBlobStore(cfg.DataRoot)`, `store.Open(cfg.DBPath, blobs)`, then `server.NewRouter(st, blobs).Run(cfg.Addr)`. cmd/ holds cobra wiring only — no disk/SQL of its own.

    Follow ./CLAUDE.md: lean Go, minimal diffs, no comments, no one-time helper abstractions.
  </action>
  <verify>
    <automated>cd /root/workspace/mywholelife && CGO_ENABLED=0 go build ./... && go vet ./cmd/... ./server/...</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go build ./...` passes (modernc.org/sqlite is pure-Go; CGO stays off).
    - `go run ./cmd/mywholelife --help` lists both `serve` and `dream`.
    - `server/router.go`, `server/register.go`, `cmd/*` import the `store` package but NOT `modernc.org/sqlite`.
  </acceptance_criteria>
  <done>Binary builds; `serve` and `dream` subcommands exist; register route is wired; seam preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Register-endpoint httptest + seam regression</name>
  <files>server/register_test.go</files>
  <read_first>
    - 02-RESEARCH.md §"httptest round-trip (write -> init) over a REAL temp-dir store" (the httptest.NewRequest + NewRecorder + router.ServeHTTP pattern, real store over t.TempDir())
    - store/store_test.go lines 1-60 (test conventions: build the real store over t.TempDir(), NewBlobStore + store.Open)
  </read_first>
  <action>
    Create `server/register_test.go` (package `server`). Build a real store: `blobs := store.NewBlobStore(t.TempDir())`; `st, _ := store.Open(filepath.Join(t.TempDir(), "idx.db"), blobs)`; `r := NewRouter(st, blobs)`.

    Test cases via `httptest.NewRequest(http.MethodPost, "/agent/register", nil)` + `httptest.NewRecorder()` + `r.ServeHTTP(w, req)`:
    (1) With `X-Agent-Name: alice` -> status 200, body is a non-empty UUID string (assert `len(strings.TrimSpace(body)) >= 32` and it contains dashes), Content-Type starts with `text/plain`.
    (2) A second register with the SAME name `alice` -> status 409 (store.ErrDuplicateName path).
    (3) With no `X-Agent-Name` header -> status 400.

    Do not import the sqlite driver here — use the store constructors only.
  </action>
  <verify>
    <automated>cd /root/workspace/mywholelife && go test ./server/ -run TestRegister -count=1 && go test ./store/ -run TestSeam -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./server/ -run TestRegister -count=1` passes all three cases.
    - `go test ./store/ -run TestSeam -count=1` (Phase-1 seam confinement) still passes — no new package imports the driver.
    - `go test ./... -count=1` passes.
  </acceptance_criteria>
  <done>Register endpoint proven: mints a UUID, rejects duplicate names (409) and missing names (400); seam intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API (`/agent/register`) | Untrusted `X-Agent-Name` header crosses here; server mints identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | caller-supplied `{id}` | mitigate | Register-first (OQ-1): identity is a server-minted UUID from `RegisterAgent`; no endpoint accepts a caller id. |
| T-02-02 | DoS | `/agent/register` flood | accept | Single-tenant MVP, no auth (D-02); note for step-2. |
| T-02-03 | Tampering | duplicate-name takeover | mitigate | `RegisterAgent` UNIQUE-name constraint -> 409; names never overwrite an existing agent row. |
| T-02-SC | Tampering | gin/cobra module installs | mitigate | Go modules (not npm/pip/cargo — slopcheck N/A). 02-RESEARCH §Package Legitimacy Audit verified gin v1.12.0 + cobra v1.10.2 live on goproxy.cn (Approved); versions pinned in go.mod/go.sum. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` passes.
- `go test ./... -count=1` passes (register tests + Phase-1 store/seam tests green).
- Manual smoke (optional): `MWL_DATA_ROOT=$(mktemp -d) go run ./cmd/mywholelife serve` then `curl -s -X POST -H 'X-Agent-Name: alice' localhost:8080/agent/register` prints a bare UUID.
</verification>

<success_criteria>
- The binary builds and runs `serve`; `dream` is a reserved stub.
- `POST /agent/register` returns a bare text/plain UUID (jq-free client parse), 409 on duplicate name, 400 on missing name.
- No package outside `store/` imports the sqlite driver.
</success_criteria>

<output>
Create `.planning/phases/02-hot-path-write-init/02-01-SUMMARY.md` when done.
</output>
