---
phase: 02-hot-path-write-init
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - client/mywholelife/SKILL.md
  - client/mywholelife/scripts/init.sh
  - client/mywholelife/scripts/push.sh
  - client/mywholelife/scripts/session_end.sh
  - client/mywholelife/scripts/install.sh
  - client/mywholelife/hook/settings.snippet.json
autonomous: true
requirements: [WRITE-02, INIT-02, CLIENT-01]

must_haves:
  truths:
    - "The `mywholelife` skill ships as a Claude Code skill folder whose `SKILL.md` documents the service URL + init/push/remind usage, with a `description` <=1024 chars (CLIENT-01, D-08)"
    - "First run of `init.sh` (no local id) registers via `POST /agent/register`, parses the bare-UUID reply WITHOUT jq, and writes `~/.mywholelife/agent.json` = {id,name,service_url} (D-10, CLIENT-01)"
    - "`init.sh` downloads `GET /agent/{id}/init` and unpacks `memory/*` -> `~/.mywholelife/memory/` and `skills/*` -> `.claude/skills/` (`--global` -> `~/.claude/skills/`) using only unzip/cp (INIT-02, D-06/D-07)"
    - "`push.sh` tars a staged project folder and POSTs it as a raw `application/x-tar` body, then clears the outbox (D-09)"
    - "The `SessionEnd` hook is dumb transport: tar each outbox folder -> POST -> clear; no curation logic (WRITE-02, D-09)"
    - "`install.sh` copies the skill into `.claude/skills/` and idempotently registers the SessionEnd hook in settings.json without clobbering existing settings (D-08)"
    - "Every client script uses only bash + curl + tar/unzip — jq is optional-only, never required (CLIENT-01)"
  artifacts:
    - path: "client/mywholelife/SKILL.md"
      provides: "skill frontmatter (name + description <=1024) + body documenting URL/init/push/remind + install/restart caveat"
      contains: "description:"
    - path: "client/mywholelife/scripts/init.sh"
      provides: "register-if-needed (jq-free UUID parse) + download+unzip init bundle into native dirs"
      contains: "/agent/register"
    - path: "client/mywholelife/scripts/push.sh"
      provides: "tar outbox folder -> curl --data-binary @- as x-tar -> clear outbox"
      contains: "application/x-tar"
    - path: "client/mywholelife/scripts/session_end.sh"
      provides: "SessionEnd hook: iterate outbox, call push, ignore stdin"
      contains: "outbox"
    - path: "client/mywholelife/scripts/install.sh"
      provides: "copy skill into .claude/skills/ + idempotent SessionEnd registration (jq-if-present else create-if-absent)"
      contains: "SessionEnd"
    - path: "client/mywholelife/hook/settings.snippet.json"
      provides: "verbatim SessionEnd settings block for manual merge / no-jq path"
      contains: "SessionEnd"
  key_links:
    - from: "client/mywholelife/scripts/init.sh"
      to: "POST /agent/register + GET /agent/{id}/init"
      via: "curl register (bare-UUID parse) then curl init -> unzip into memory/ + .claude/skills/"
      pattern: "agent/register|agent/\\$ID/init|/init"
    - from: "client/mywholelife/scripts/session_end.sh"
      to: "push.sh -> POST /agent/{id}/memory"
      via: "hook tars each outbox folder and pushes it, then clears the outbox"
      pattern: "push\\.sh|/memory"
---

<objective>
Ship the `mywholelife` client: a dependency-free Claude Code skill folder (SKILL.md + bash scripts) that registers identity, pushes curated project memory on `SessionEnd`, and reconstitutes the init bundle into native Claude Code locations.

Purpose: Deliver WRITE-02 (SessionEnd dumb-transport hook), INIT-02 (unpack bundle into memory dir + `.claude/skills/`), and CLIENT-01 (bash/curl/tar only, jq optional) against the locked write/init contract (D-05/D-06/D-07) and the register endpoint from Plan 02-01.

Output: `client/mywholelife/` — SKILL.md, `scripts/{init,push,session_end,install}.sh`, and a `settings.snippet.json` hook block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hot-path-write-init/02-CONTEXT.md
@.planning/phases/02-hot-path-write-init/02-RESEARCH.md

<contract>
<!-- The locked server contract these scripts target (D-05/D-06/D-07/D-10 + Plan 02-01/02-02). No Go import — bash only. -->
Register: POST /agent/register  header X-Agent-Name: <name>  -> body is a BARE UUID (text/plain), no JSON, no jq.
Write:    POST /agent/{id}/memory?scope=project&project=<name>   body = raw tar   header Content-Type: application/x-tar   -> 204
          POST /agent/{id}/memory?scope=global                   body = raw tar   Content-Type: application/x-tar        -> 204
Init:     GET  /agent/{id}/init  -> a .zip with:  memory/global/… , memory/long-term-memory.md , skills/<memId>/…
Identity: ~/.mywholelife/agent.json = {"id":…,"name":…,"service_url":…}
Outbox:   agent curates skill folders under ~/.mywholelife/outbox/<project>/ during the session (curation is the agent's job, not the hook's).
</contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md + init.sh + push.sh (identity, unpack, curated push)</name>
  <files>client/mywholelife/SKILL.md, client/mywholelife/scripts/init.sh, client/mywholelife/scripts/push.sh</files>
  <read_first>
    - 02-RESEARCH.md §"Dependency-free client idioms (bash + curl + tar/unzip)" (register bare-UUID parse, push --data-binary @-, init curl|unzip idioms)
    - 02-RESEARCH.md §"State of the Art" (description <=1024 is a safe subset of the 1536 cap — keep 1024), §"Pitfall 5" (live change detection: creating a top-level `.claude/skills/` mid-session needs a restart — document it)
    - .planning/phases/00-skill-context-validation-spike/00-SPIKE-FINDINGS.md (the naive SKILL.md frontmatter shape: name + description)
  </read_first>
  <action>
    Create `client/mywholelife/SKILL.md`: YAML frontmatter with `name: mywholelife` and a `description` (<=1024 chars, D-08/CLIENT-01) summarizing "persistent long-term memory for Claude Code — run init at session start to reload memory; curated writes push on session end." Body documents: the service URL (read from `MWL_SERVICE_URL` env or `~/.mywholelife/agent.json`), how to run `init.sh` (and the restart caveat from Pitfall 5), how curation works (write skill folders into `~/.mywholelife/outbox/<project>/` during the session), and that `remind` is a Phase-3 addition (mention as forthcoming, do not implement).

    Create `client/mywholelife/scripts/init.sh` (bash, `set -euo pipefail`):
    - Resolve `SERVICE_URL` (arg/env/agent.json) and `AGENT_JSON=~/.mywholelife/agent.json`.
    - If `agent.json` is missing: read a name (arg or `$MWL_AGENT_NAME` or hostname), `ID=$(curl -sf -X POST -H "X-Agent-Name: $NAME" "$SERVICE_URL/agent/register")` — parse the BARE UUID with plain command substitution (no jq); `printf '{"id":"%s","name":"%s","service_url":"%s"}\n'` to `agent.json`. If `agent.json` exists, read `id` from it WITHOUT jq (a `grep -oE '"id":"[0-9a-f-]{36}"'` / `sed` extraction, or `. "$AGENT_JSON"`-free parsing) — keep jq optional-only.
    - Support `--global` to set `SKILLS_DIR=~/.claude/skills` (default `.claude/skills`).
    - Download + unpack (D-06/D-07): `tmp=$(mktemp -d)`; `curl -sf "$SERVICE_URL/agent/$ID/init" -o "$tmp/init.zip"`; `unzip -o "$tmp/init.zip" -d "$tmp/x"`; `mkdir -p ~/.mywholelife/memory "$SKILLS_DIR"`; `cp -R "$tmp/x/memory/." ~/.mywholelife/memory/`; `[ -d "$tmp/x/skills" ] && cp -R "$tmp/x/skills/." "$SKILLS_DIR/"`. Print the restart caveat if `.claude/skills/` was newly created.

    Create `client/mywholelife/scripts/push.sh` (bash, `set -euo pipefail`): args = project name + optional scope (default project). Resolve id/URL from `agent.json`. `OUTBOX=~/.mywholelife/outbox`; `tar -C "$OUTBOX" -cf - "$PROJECT" | curl -sf --data-binary @- -H 'Content-Type: application/x-tar' "$SERVICE_URL/agent/$ID/memory?scope=$SCOPE&project=$PROJECT"`; on curl success `rm -rf "$OUTBOX/$PROJECT"` (clear only after a successful POST). No jq.

    All scripts: bash + curl + tar/unzip only; make them `chmod +x`-friendly (shebang `#!/usr/bin/env bash`).
  </action>
  <verify>
    <automated>cd /root/workspace/mywholelife && bash -n client/mywholelife/scripts/init.sh && bash -n client/mywholelife/scripts/push.sh && ! grep -qw jq client/mywholelife/scripts/init.sh client/mywholelife/scripts/push.sh && grep -q '^description:' client/mywholelife/SKILL.md && [ "$(grep -m1 '^description:' client/mywholelife/SKILL.md | cut -c14- | wc -c)" -le 1024 ]</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` (syntax check) passes for `init.sh` and `push.sh`.
    - Neither script contains a hard `jq` dependency (grep finds no `jq`).
    - `SKILL.md` has a `description:` frontmatter field <=1024 chars.
    - `push.sh` posts with `Content-Type: application/x-tar` via `--data-binary @-` and clears the outbox only after a successful curl.
    - `init.sh` registers on first run (bare-UUID parse, no jq) and unzips `memory/*` + `skills/*` into the native dirs.
  </acceptance_criteria>
  <done>The identity bootstrap, curated push, and native-location unpack are implemented dependency-free and syntax-clean.</done>
</task>

<task type="auto">
  <name>Task 2: SessionEnd hook + install.sh + settings snippet</name>
  <files>client/mywholelife/scripts/session_end.sh, client/mywholelife/scripts/install.sh, client/mywholelife/hook/settings.snippet.json</files>
  <read_first>
    - 02-RESEARCH.md §"Claude Code SessionEnd hook — settings.json registration (VERIFIED contract)" (verbatim block; stdin keys session_id/transcript_path/cwd/hook_event_name/reason; non-blocking, side-effects only; matcher "*")
    - 02-RESEARCH.md §"Idempotent hook registration in install.sh (jq-optional)" (jq-if-present merge else create-if-absent + grep idempotency guard, A4)
  </read_first>
  <action>
    Create `client/mywholelife/hook/settings.snippet.json`: the verbatim SessionEnd block — `{"hooks":{"SessionEnd":[{"matcher":"*","hooks":[{"type":"command","command":"${CLAUDE_PROJECT_DIR}/.claude/skills/mywholelife/scripts/session_end.sh"}]}]}}`.

    Create `client/mywholelife/scripts/session_end.sh` (bash, `set -euo pipefail`): DUMB TRANSPORT only (D-09/WRITE-02) — it may ignore stdin entirely (the SessionEnd JSON on stdin is not needed for tar-and-push). `OUTBOX=~/.mywholelife/outbox`; if the outbox has no entries, exit 0. For each immediate subdirectory `<project>` under `$OUTBOX`, call `push.sh "<project>"` (which tars, POSTs, and clears on success). Contains NO curation/"what to remember" logic. Exit 0 on success; SessionEnd is non-blocking so failures must not error the session hard (log to stderr, keep going).

    Create `client/mywholelife/scripts/install.sh` (bash, `set -euo pipefail`): (1) copy the skill folder into `.claude/skills/mywholelife/` (or `~/.claude/skills/mywholelife/` with `--global`), `chmod +x` the scripts. (2) Register the SessionEnd hook in `.claude/settings.json` idempotently (A4): if `jq` is present and settings exist, use jq to append the SessionEnd entry ONLY if no existing SessionEnd entry already points at our `session_end.sh` command (grep-guard before appending to avoid duplicates); if `jq` is absent, only write the snippet as a fresh `settings.json` when none exists (never clobber a user's settings) and print an instruction to merge `hook/settings.snippet.json` manually otherwise. Print the Pitfall-5 restart note (a first-time top-level `.claude/skills/` needs a Claude Code restart to load).
  </action>
  <verify>
    <automated>cd /root/workspace/mywholelife && bash -n client/mywholelife/scripts/session_end.sh && bash -n client/mywholelife/scripts/install.sh && python3 -c "import json,sys; json.load(open('client/mywholelife/hook/settings.snippet.json'))" && grep -q 'SessionEnd' client/mywholelife/hook/settings.snippet.json && ! grep -qE 'remember|curat' client/mywholelife/scripts/session_end.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` passes for `session_end.sh` and `install.sh`.
    - `settings.snippet.json` is valid JSON containing a `SessionEnd` hook pointing at `session_end.sh`.
    - `session_end.sh` contains no curation/"remember" logic — it only iterates the outbox and delegates to `push.sh` (dumb transport, D-09).
    - `install.sh` registers the hook idempotently (jq-if-present with a duplicate guard; create-if-absent otherwise) and never clobbers an existing `settings.json`.
  </acceptance_criteria>
  <done>The SessionEnd hook (dumb transport) and its idempotent, jq-optional installer are in place with a valid settings snippet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `.zip` -> client `unzip` | Downloaded archive is expanded into `~/.mywholelife/memory/` and `.claude/skills/` |
| install.sh -> user's `.claude/settings.json` | Hook registration mutates a user-owned config file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering / Elevation | zip-slip on client `unzip` | mitigate | Server sanitizes entry names at ingest + transcode (Plan 02); modern `unzip` also refuses `../`. Client unpacks into dedicated dirs only. |
| T-02-09 | Tampering | install.sh clobbering user settings.json | mitigate | jq-if-present merge with a grep duplicate-guard; create-if-absent otherwise; never overwrite existing settings. |
| T-02-10 | Information Disclosure | SessionEnd hook exfiltrating data | accept | Dumb transport pushes only what the agent staged in the outbox to the single-tenant server (D-02/D-09); curation is the agent's responsibility. |
</threat_model>

<verification>
- `bash -n` passes for all four scripts.
- `client/mywholelife/hook/settings.snippet.json` is valid JSON with a SessionEnd hook.
- No client script hard-depends on `jq` (grep finds none in the required paths).
- `SKILL.md` `description` <=1024 chars.
</verification>

<success_criteria>
- The `mywholelife` skill documents the URL + init/push/remind usage and installs a SessionEnd hook (WRITE-02, CLIENT-01, D-08).
- `init.sh` registers (jq-free) and reconstitutes the bundle into native Claude Code locations (INIT-02, D-06/D-07/D-10).
- The SessionEnd hook is dumb transport: tar outbox -> POST -> clear (D-09).
- All scripts are bash + curl + tar/unzip only.
</success_criteria>

<output>
Create `.planning/phases/02-hot-path-write-init/02-03-SUMMARY.md` when done.
</output>
